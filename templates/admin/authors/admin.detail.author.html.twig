{% extends 'base.html.twig' %}

{% block body %}
    <h2>Автор "<span id="detail-author-header">{{ author.name }}</span>"</h2>
    <div class="detail-book">
        <div class="book-item">
            <p>Имя</p>
            <input id="author-name" type="text" value="{{ author.name }}">
        </div>
        <div class="book-buttons flex-container flex-column">
            <button class="book-button" id="update-author-button">Обновить</button>
            <button class="book-button" id="delete-author-button">Удалить</button>
        </div>
    </div>
    <script type="text/javascript">
        let authorId = {{ author.id }}
    </script>
{% endblock %}