{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center mx-auto">
        <h2>Автор "<span id="detail-author-header">{{ author.name }}</span>"</h2>
        <div class="detail-book col-12">
            <div class="book-item col-6 mx-auto">
                <p>Имя</p>
                <input id="author-name" type="text" value="{{ author.name }}" class="form-control">
            </div>
            <div class="book-buttons flex-container flex-column mx-auto">
                <button class="book-button btn btn-primary" id="update-author-button">Обновить</button>
                <button class="book-button btn btn-primary" id="delete-author-button">Удалить</button>
            </div>
        </div>
    </div>
    <script type="text/javascript">
        let authorId = {{ author.id }}
    </script>
{% endblock %}